<% if logged_in? %>
 	<strong><%= current_user.login %></strong> |
    <!--
	<%= link_to "Dashboard", root_url %> |
    -->
	<%= link_to "My Account", edit_user_path( current_user ) %> |
	<%= link_to "Log out", logout_url, :method => :delete %>
<% else %>
	<%= link_to "Sign up", signup_url %> | 
	<%= link_to "Log in", login_url %>
<% end %>

    | <%= link_to 'English', url_for( params.merge( {:locale => 'en'} ) ) %>
    | <%= link_to 'Portuguese', url_for( params.merge( {:locale => 'pt-BR'} ) ) %>